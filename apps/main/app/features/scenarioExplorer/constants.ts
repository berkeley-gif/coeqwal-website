/**
 * Scenario explorer constants shared across the feature.
 */

/**
 * The canonical "today's operations" baseline scenario. Used as the
 * reference point for comparisons in EquityPanel and as the pre-pin on
 * first visit so Radar / Distribution / Resilience are never empty.
 */
export const BASELINE_SCENARIO_ID = "s0020"
