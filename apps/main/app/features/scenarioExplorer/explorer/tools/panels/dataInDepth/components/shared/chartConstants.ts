/**
 * Shared layout constants for Data in depth chart sections.
 */

/**
 * Pixel size of the per-scenario tier glyph charts in the section headers
 * (CWS, AG, reservoir storage). Matches the md+ tier glyph size used across
 * the scenario explorer tools and Learn map panels.
 */
export const TIER_CHART_SIZE = 60
