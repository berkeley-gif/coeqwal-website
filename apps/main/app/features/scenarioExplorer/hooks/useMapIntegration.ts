/**
 * Hook that manages map integration for the scenario explorer
 * Tier visualization is now handled by TierMarkers component
 */
export function useMapIntegration(
  showMapView: boolean,
  anySelectedOutcome: string | null,
) {
  // Map integration for scenario explorer
  // Tier visualizations are handled directly by TierMarkers component
  // No additional layers needed here

  return {}
}
