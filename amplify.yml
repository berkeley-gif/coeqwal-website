version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm install 22
            - nvm use 22
            - node -v
            - corepack enable
            - pnpm install
        build:
          commands:
            - pnpm rebuild
            - npx turbo run build --filter=main
            - pnpm --filter=main export
      artifacts:
        baseDirectory: apps/main/out
        files:
          - '**/*'
      buildPath: /
    appRoot: apps/main
    environment:
      variables:
        NODE_VERSION: 22