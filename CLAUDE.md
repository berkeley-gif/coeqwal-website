# Claude Code Rules

## Build

- Do NOT run builds after minor or stylistic changes. Only build when verifying type errors, structural changes, or when explicitly asked.

## Git

- NEVER commit or push. The user will handle all git operations.
